// we can use the logic of ceil and floor here

public static int kthlargest(Node node,int k)
{
  floor=Integer.MIN_VALUE;
  int factor=Integer.MAX_VALUE;
  for(int i=0;i<k;i++)
  { 
  ceilandfloor(node,factor);
 factor=floor;
  floor=Integer.MIN_VALUE;
 } 
 return factor;
} 
